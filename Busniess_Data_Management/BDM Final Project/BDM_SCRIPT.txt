mysql> USE HOTEL
Database changed

mysql> CREATE TABLE CUSTOMER (
    -> ID_NUMBER INT(11),
    -> CUSTOMER_TYPE VARCHAR(150) NOT NULL,
    -> CUSTOMER_NAME VARCHAR(150) NOT NULL,
    -> CUSTOMER_AGE INT,
    -> ID_TYPE VARCHAR(150) NOT NULL,
    -> PAYMENT_MODE VARCHAR(100) NOT NULL,
	-> PRIMARY KEY (ID_NUMBER));
	
	
	
mysql> CREATE TABLE DEPENDENT (
    -> DEPENDENT_ID INT NOT NULL,
    -> DEPENDENT_NAME VARCHAR(50) NOT NULL,
    -> DEPENDENT_AGE INT(11),
    -> DEPTID_TYPE VARCHAR(50),
    -> DEPTID_NUMBER INT(11),
	-> ID_NUMBER INT(11),
	-> PRIMARY KEY (DEPENDENT_ID),
    -> FOREIGN KEY (ID_NUMBER) REFERENCES CUSTOMER(ID_NUMBER));
	
mysql> CREATE TABLE EVENT (
    -> EVENT_ID INT(11) NOT NULL AUTO_INCREMENT,
    -> EVENT_NAME VARCHAR(50) NOT NULL,
    -> EVENT_LOCATION VARCHAR(50),
    -> NUM_OF_PARTICIPANTS INT(11),
    -> EVENT_DATE DATE NOT NULL,
	-> EVENT_NOTES VARCHAR(50),
	-> EVENT_TYPE VARCHAR(50),
    -> EVENT_PRICE FLOAT NOT NULL,
	-> PRIMARY KEY (EVENT_ID));	
	
mysql> CREATE TABLE ROOM (
    -> FLOOR INT(11),
    -> ROOM_NUMBER INT(11) NOT NULL,
    -> ROOM_TYPE VARCHAR(50),
    -> ROOM_PRICE INT(11) NOT NULL,
    -> INROOM_BAR VARCHAR(50),
    -> EVENT_ID INT NOT NULL,
	-> IS_AVAILABLE INT(11),
	-> PRIMARY KEY (ROOM_NUMBER),
	-> FOREIGN KEY (EVENT_ID) REFERENCES EVENT (EVENT_ID));
	
	
mysql> CREATE TABLE RESERVATION (
	-> EVENT_ID INT,
	-> ID_NUMBER INT(11) NOT NULL,	
	-> BOOKING_DATE DATE NOT NULL,
	-> ROOM_NUMBER INT(11) NOT NULL,
	-> TRANSACTION_ID INT(11) NOT NULL,
	-> TOTAL_AMOUNT FLOAT,
	-> PRIMARY KEY (TRANSACTION_ID),
	-> FOREIGN KEY (ROOM_NUMBER) REFERENCES ROOM(ROOM_NUMBER),
	-> FOREIGN KEY (EVENT_ID) REFERENCES EVENT (EVENT_ID),
	-> FOREIGN KEY (ID_NUMBER) REFERENCES CUSTOMER (ID_NUMBER));